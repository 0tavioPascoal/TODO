//css
import styles from './header.module.css'

const Header = () => {
  return (
    <header className={styles.header}>
            <h1> TODO com React + TypeScript</h1>
        </header>
  )
}

export default Header